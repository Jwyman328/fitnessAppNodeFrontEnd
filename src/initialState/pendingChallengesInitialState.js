import React from 'react';

const initialState = {
    isLoading:false,
    isError:false,
    pendingChallenges:[],
    updateisLoading: true,
    upDateIsError:false,
}

export default initialState;